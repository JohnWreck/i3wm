#!/bin/bash

curl -s http://wttr.in/Croatia+Zagreb?format="%t+%p+%P+%w" 2>&1 > /tmp/weather_report

w=$(cat /tmp/weather_report)

if echo "$w" | grep -ie 'Unknown\|Sorry' >/dev/null; then

 echo "Unknown"

else 

 echo "$w"

fi

rm /tmp/weather_report 
